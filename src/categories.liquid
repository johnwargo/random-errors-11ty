---
title: Categories
layout: default
---

{% assign categories = collections.categories | sort %}

<header>
  <h2>{{ title }}</h2>
  <p>View all of the posts for a particular category by clicking on one of the categories listed below. There are {{ categories.length }} categories on the site today.</p>
</header>

{% for category in categories %}
  <ul>
    <li><a href="{{ "/" | htmlBaseUrl }}category/{{ category | slugify }}">{{ category | capitalize }}</a></li>
  </ul>
{% endfor %}